import type { PreCallDiagnosticOptions, PreCallDiagnosticReport, PreCallDiagnosticRunner } from './types';
export declare class PreCallDiagnostic implements PreCallDiagnosticRunner {
    private readonly options;
    constructor(options: PreCallDiagnosticOptions);
    run(): Promise<PreCallDiagnosticReport>;
    private runFull;
    private runNetworkOnly;
    private runTest;
    private runMicrophoneOnly;
    private waitForCallEstablishment;
    private createDiagnosticCall;
    private collectSamples;
}
