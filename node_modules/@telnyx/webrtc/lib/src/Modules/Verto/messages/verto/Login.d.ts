import BaseRequest from './BaseRequest';
declare class Login extends BaseRequest {
    method: string;
    constructor(login: string, passwd: string, login_token: string, sessionid: string, userVariables: Record<string, any>, reconnection: boolean, earlySdpAnswer?: boolean);
}
export { Login };
